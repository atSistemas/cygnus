spring:
  application:
    name: admin-server
  cloud:
    config:
      failFast: true
      uri: ${CONFIG_SERVER_ENDPOINT}
      retry:
        initialInterval: ${CONFIG_RETRY_INITIAL_INTERVAL}
      