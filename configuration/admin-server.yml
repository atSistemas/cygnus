# HTTP Server
server:
  port: 8080

management:
  security:
    enabled: false
    
# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8080/eureka/
  instance:
     preferIpAddress: true
